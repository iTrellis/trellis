syntax = "proto3";

package service;

enum EventType {
    create = 0;
    delete = 1;
    update = 2;
}

enum RegisterType {
    memory = 0;
    etcd   = 1;
}

enum TransportType {
    GRPC = 0;
    HTTP = 1;
    QUIC = 2;
}